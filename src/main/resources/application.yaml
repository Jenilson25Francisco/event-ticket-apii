spring:
  application:
    name: event-api

  datasource:
    url: jdbc:postgresql://localhost:5532/eventdb
    username: postgres
    password: postgres

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect

